apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-app-chart.fullname" . }}
  labels:
    {{- include "eks-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-app-chart.selectorLabels" . | nindent 4 }}
